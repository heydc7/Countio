#include<stdio.h>
#include<stblin.h>

  int main()
  {
     int i,fact=1,n;
     printf("Enter A Number");
     scanf("%d",&n);
     for(i=1,i<=n,i++)
        {
          fact=fact*i;
        }
      printf("Factorial of %d is %d",n,fact);
      return 0;
   }
  
